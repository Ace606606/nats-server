{
    "version": "0.2.0",
    "configurations": [
        /// ======================
        //   DEBUG BUILD
        // ======================
        {
            "name": "Launch my_executable (Debug)",
            "type": "cppdbg",
            "request": "launch",
            //"program": "${workspaceFolder}/build-debug/my_executable",
            "program": "${command:cmake.launchTargetPath}",
            //"program": "${workspaceFolder}/build-debug/test",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            //"preLaunchTask": "CMake: Build (Debug)"
        }
    ]
}